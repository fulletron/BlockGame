#include "stage.h"

namespace GS {
namespace Stage {

// BASE CLASS IN A DIFFERENT PROJECT ::
// DO NOT DEFINE THINGS HERE :: 

/*
* We can't define functionality in this CPP, because then the functionality is only linked in
* this project. Because we derive stages in game, we can't have the base stage definitions in
* a cpp. The .h carries over and goes everywhere, so define everything in there.
*
*
*/

};
};