/*

#include "gamestate.h"

/// INSTR :: THIS INCLUDE LIST IS FOR EACH INDIVIDUAL PANEL POSSIBLY ON THE SCREEN
//#include "..\..\Panel\worldpanel.h"

namespace GS {
namespace StateNS {
	_UINT32 GameState::init(GS::Game* a_rpGame)
	{
		m_rpGame = a_rpGame;

		_GSVECTOR(PanelNS::IPanel *) t_visiblePanels;
		_GSVECTOR(PanelNS::IPanel *) t_allPanels;

		/// INSTR :: THIS IS WHERE PANELS GET ASSIGNED (AND NEW'D), 
		/// ADDED TO A LIST OF ALL AND VISIBLE, AND THEN INIT'D THROUGH
		/// THE PANEL MANAGER init FUNCTION.
		//TEST_myTestPanel = new GS::PanelNS::WorldPanel();

		//m_panelManager.init(m_rpGame, t_visiblePanels, t_allPanels);

		return 0;
	}
};
};

*/