#include "panelbackground.h"

/// INSTR :: EVERY ENTITY IN THE PANEL NEEDS TO BE INCLUDED
//#include "layerbackgroundtext.h"

/// PANEL NEEDS A FRAMEBUFFER, AND LAYERMANAGER
namespace GS {
namespace PanelNS {
	/*
	_UINT32 PanelBackground::init(GS::Game * a_rpGame)
	{
		m_rpGame = a_rpGame;

		/// INSTR :: SET THESE THREE TO WHAT THEY SHOULD BE!
		m_isInputGate = false;
		m_isDrawGate = false;
		setMyBaseFocus(0);

		//_GSVECTOR(PanelNS::ILayer *) t_allLayers;

		/// INSTR :: NEW EVERY LAYER... INIT IT?
		//m_pLayerBackgroundText = new GS::PanelNS::LayerBackgroundText();
		//m_layerManager.init(m_rpGame, this, t_allLayers);

		return 0;
	}
	*/
};
};