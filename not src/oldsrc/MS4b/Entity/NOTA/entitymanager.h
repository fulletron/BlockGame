#ifndef __ENTITYMANAGER_H_
#define __ENTITYMANAGER_H_

#include "..\..\Game\definitions.h"

namespace GS { class Game;
namespace InputNS { class Input; };
namespace EntityNS { class Entity;
/*
	class EntityManager
	{
		public:
			typedef _GSVECTOR(Entity*) EntPtrVector;

		private:
			GS::Game * m_rpGame;
			EntPtrVector m_allEntities;

		public:
			_UINT32 init(GS::Game * const a_rpGame);
			void render();
			void input(const GS::InputNS::Input &, bool &, bool &);
			void update(_DOUBLE a_dt);
			void shutdown();
			void onScreenResize();
			void addEntity(Entity * const a_pEntity);
			void removeEntity(Entity * const a_pEntity);
	};
	*/
};
};

#endif